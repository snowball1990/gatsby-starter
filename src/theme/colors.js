export default {
  // 主色调
  text:{
    // 强调色（ 用于标题 ）
    accent: "#292929",
    // 主要色（ 用于主体文字 ）
    primary: "#292929",
    // 修饰色（ 用于次要文字，包括标题说明、图片说明等 ）
    muted: "#757575",
  },
  background:{
    // 主要色（ 用于主体背景 ）
    primary: "#ffffff",
    // 修饰色（ 用于次要背景，包括弹出层等 ）
    muted: "#ffffff",
    // 块背景色
    section: "#f2f2f2",
    // 反转背景
    card: "#fafafa"
  },
  modes:{
    dark: {
      // 主色调
      text:{
        // 强调色（ 用于标题 ）
        accent: "#ffffff",
        // 主要色（ 用于主体文字 ）
        primary: "#d9d9d9",
        // 修饰色（ 用于次要文字，包括标题说明、图片说明等 ）
        muted: "#a9a9a9",
      },
      // 博客背景配色
      background:{
        // 主要色（ 用于主体背景 ）
        primary: "#121212",
        // 修饰色（ 用于次要背景，包括弹出层等 ）
        muted: "#191919",
        // 块背景色
        section: "#262626",
        // 反转背景
        card: "#262626"
      }
    }
  }
}